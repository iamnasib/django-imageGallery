This is an Image Gallery webapp
1. After downloading, unzip the file and install requirements.txt file using pip
2. Open CMD at the location same as requirements.txt
3. Run the following commands:
> py manage.py migrate
> py manage.py runserver